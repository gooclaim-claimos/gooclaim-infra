# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
*.so

# Virtual environments
.venv/
venv/
env/
.env

# Testing
.tox/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.coveragerc

# Type checking
.pyright/
pyrightconfig.json

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Secrets — never commit these
*.pem
*.key
*.p12
*.pfx
.env.*
secrets.yml
secrets.yaml
kubeconfig
*kubeconfig*

# Generated
/generated/
*.pb.go
*_pb2.py
*_pb2_grpc.py

# Terraform — never commit these
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
*.tfvars
*.tfplan
*.tfplan.binary
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# Keep .terraform.lock.hcl tracked — providers lock file for reproducibility

# Azure CLI + kubectl — local caches with sensitive tokens
.azure/
.kube/cache/
.kube/config
azure-cli-*.log

# Per-env secrets snapshots (in case anyone exports them to disk)
secrets-dev.txt
secrets-nprd.txt
secrets-prod.txt
*-account-key.txt
*-tfstate-key.txt
machine-jwt-*.txt

# Docker
.dockerignore

# Local-only task tracking (not committed to gooclaim-infra)
tasks/
Gooclaim-Sheets/
